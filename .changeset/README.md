# Changesets

- Run `npm run changeset` to create a release note and version intent file.
- Run `npm run release:version` to apply pending version updates across the workspace.
- Run `npm run release:publish` to build and publish the packages to npm.
- `access` is configured as `public` for the scoped packages in this repository.
